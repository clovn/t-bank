package com.example.tbank.domain.model

enum class NotificationType {
    DEFAULT,
    INVITATION,
    PARTICIPANT_DELETED,
    INVITATION_ACCEPTED,
    INVITATION_DECLINED,
    DEBT_REMINDER,
    EXPENSE_ADD
}